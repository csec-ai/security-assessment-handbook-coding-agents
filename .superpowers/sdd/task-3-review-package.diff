diff --git a/index.html b/index.html
index b5fb3cb..0c0e0b6 100644
--- a/index.html
+++ b/index.html
@@ -91,37 +91,39 @@
     /* ── Tabs ── */
     .tab-nav {
       display: flex; flex-wrap: wrap; background: var(--bg-panel); border-radius: 12px 12px 0 0;
-      box-shadow: var(--shadow-sm); border-bottom: 2px solid var(--border);
+      box-shadow: var(--shadow-sm); border-bottom: 1px solid var(--border);
+      padding: 10px 16px; gap: 6px;
     }
     .tab-btn {
-      flex: 1; padding: 16px 28px; border: none; background: transparent; cursor: pointer;
-      font-size: 0.9rem; font-weight: 500; color: var(--text-muted); text-align: center;
-      border-bottom: 3px solid transparent; margin-bottom: -2px; transition: color 0.18s, background 0.18s, border-color 0.18s; white-space: nowrap;
+      flex: 1; padding: 10px 24px; border: none; background: transparent; cursor: pointer;
+      font-size: 0.875rem; font-weight: 500; color: var(--text-muted); text-align: center;
+      border-radius: 8px; text-transform: uppercase; letter-spacing: 0.06em;
+      transition: color 0.18s, background 0.18s; white-space: nowrap;
     }
-    .tab-btn:hover { color: var(--text-primary); background: rgba(0,0,0,0.04); }
-    .tab-btn.active { color: #2b6cb0; border-bottom-color: #2b6cb0; background: var(--bg-panel); font-weight: 700; }
-    .tab-panel { display: none; background: var(--bg-panel); border-radius: 0 0 12px 12px; box-shadow: var(--shadow-md); padding: 32px; }
+    .tab-btn:hover { color: var(--text-primary); background: var(--bg-subtle); }
+    .tab-btn.active { color: #ffffff; background: var(--accent); font-weight: 600; }
+    .tab-panel { display: none; background: var(--bg-panel); border-radius: 0 0 12px 12px; box-shadow: var(--shadow-md); padding: 36px; }
     .tab-panel.active { display: block; }
     .pane-title { font-size: 1.1rem; font-weight: 700; color: var(--text-primary); margin-bottom: 6px; }
     .pane-desc { font-size: 0.875rem; color: var(--text-muted); line-height: 1.65; margin-bottom: 24px; }
     .section-label { font-size: 0.72rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--text-muted); margin-bottom: 10px; }
 
     /* ── Diagram toggle ── */
-    .diagram-toggle { display: flex; margin-bottom: 20px; border: 1.5px solid #e2e8f0; border-radius: 10px; overflow: hidden; width: fit-content; }
+    .diagram-toggle { display: flex; margin-bottom: 20px; border: 1px solid #e2e8f0; border-radius: 10px; overflow: hidden; width: fit-content; }
     .dtoggle-btn { padding: 10px 24px; border: none; cursor: pointer; font-size: 0.875rem; font-weight: 600; color: #718096; background: #f7fafc; transition: all 0.15s; }
-    .dtoggle-btn:first-child { border-right: 1.5px solid #e2e8f0; }
-    .dtoggle-btn.active { background: #2b6cb0; color: white; }
+    .dtoggle-btn:first-child { border-right: 1px solid #e2e8f0; }
+    .dtoggle-btn.active { background: var(--accent); color: white; }
     .dtoggle-btn:hover:not(.active) { background: #edf2f7; }
 
     /* ── Architecture layout ── */
     .arch-layout { display: grid; grid-template-columns: 1fr 340px; gap: 24px; align-items: start; }
     @media (max-width: 1100px) { .arch-layout { grid-template-columns: 1fr; } }
 
-    .diagram-wrapper { background: var(--bg-subtle); border: 1.5px solid var(--border); border-radius: 10px; padding: 16px; overflow-x: auto; }
+    .diagram-wrapper { background: var(--bg-subtle); border: 1px solid var(--border); border-radius: 12px; padding: 16px; overflow-x: auto; }
     .diagram-wrapper svg { display: block; }
 
     /* ── Info panel ── */
-    .info-panel { background: var(--bg-subtle); border: 1.5px solid var(--border); border-radius: 10px; padding: 20px; min-height: 300px; }
+    .info-panel { background: var(--bg-subtle); border: 1px solid var(--border); border-radius: 12px; padding: 20px; min-height: 300px; }
     .info-panel-empty { display: flex; flex-direction: column; align-items: center; justify-content: center; height: 280px; color: var(--text-faint); text-align: center; }
     .info-panel-empty .icon { font-size: 2rem; margin-bottom: 12px; opacity: 0.5; }
     .info-panel-empty p { font-size: 0.875rem; line-height: 1.6; }
@@ -145,13 +147,13 @@
     .mit-info-item::before { content: '→ '; color: #38a169; font-weight: 700; }
 
     /* ── Threat vector filters (arch tab) ── */
-    .threat-filter-section { background: var(--bg-subtle); border: 1.5px solid var(--border); border-radius: 10px; padding: 16px 18px; margin-bottom: 20px; }
+    .threat-filter-section { background: var(--bg-subtle); border: 1px solid var(--border); border-radius: 12px; padding: 16px 18px; margin-bottom: 20px; }
     .threat-filter-section h3 { font-size: 0.8rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text-secondary); margin-bottom: 12px; }
     .threat-filter-grid { display: flex; flex-wrap: wrap; gap: 8px; }
     .tf-btn {
       display: flex; flex-direction: column; gap: 2px;
       padding: 8px 14px; border-radius: 8px;
-      border: 1.5px solid var(--tf-stroke, #e2e8f0);
+      border: 1px solid var(--tf-stroke, #e2e8f0);
       background: var(--bg-panel); cursor: pointer; text-align: left;
       transition: all 0.15s; min-width: 0;
     }
@@ -181,7 +183,7 @@
 
     /* Likelihood chips */
     .likelihood-row { display: flex; gap: 8px; margin-top: 12px; }
-    .lh-chip { flex: 1; text-align: center; padding: 8px 6px; border-radius: 8px; border: 1.5px solid #e2e8f0; }
+    .lh-chip { flex: 1; text-align: center; padding: 8px 6px; border-radius: 8px; border: 1px solid #e2e8f0; }
     .lh-chip .lh-label { font-size: 0.68rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #a0aec0; margin-bottom: 3px; }
     .lh-chip .lh-val   { font-size: 0.85rem; font-weight: 700; }
     .lh-val.likely   { color: #c05621; }
@@ -190,9 +192,9 @@
 
     /* ── TB legend ── */
     .tb-legend { display: flex; flex-wrap: wrap; gap: 6px; }
-    .tb-legend-item { display: flex; align-items: center; gap: 6px; font-size: 0.78rem; color: var(--text-secondary); cursor: pointer; padding: 4px 10px; border-radius: 8px; border: 1.5px solid transparent; transition: all 0.15s; }
+    .tb-legend-item { display: flex; align-items: center; gap: 6px; font-size: 0.78rem; color: var(--text-secondary); cursor: pointer; padding: 4px 10px; border-radius: 8px; border: 1px solid transparent; transition: all 0.15s; }
     .tb-legend-item:hover { background: var(--bg-subtle); border-color: #c3dafe; }
-    .tb-legend-item.active { background: var(--row-active); border-color: #3182ce; }
+    .tb-legend-item.active { background: var(--row-active); border-color: var(--accent); }
     .tb-legend-dot { width: 12px; height: 12px; border-radius: 3px; border: 2px solid; flex-shrink: 0; }
 
     /* ── Attack chain ── */
@@ -200,7 +202,7 @@
     .threat-sel-btn {
       display: flex; flex-direction: column; gap: 2px;
       padding: 8px 14px; border-radius: 8px;
-      border: 1.5px solid var(--ts-stroke, #e2e8f0);
+      border: 1px solid var(--ts-stroke, #e2e8f0);
       background: var(--bg-panel); cursor: pointer; text-align: left; transition: all 0.15s;
     }
     .threat-sel-btn:hover { border-color: var(--ts-stroke,#3182ce); background: var(--ts-fill,#ebf8ff); }
@@ -211,7 +213,7 @@
     .threat-sel-btn .ts-name { font-size: 0.82rem; font-weight: 600; color: var(--text-primary); line-height: 1.3; }
     .threat-sel-btn.active .ts-name { color: white; }
 
-    .attack-overview { background: var(--row-active); border: 1.5px solid #bee3f8; border-radius: 10px; padding: 18px 20px; margin-bottom: 20px; border-left: 4px solid #3182ce; }
+    .attack-overview { background: var(--row-active); border: 1px solid #bee3f8; border-radius: 12px; padding: 18px 20px; margin-bottom: 20px; border-left: 4px solid #3182ce; }
     .attack-overview h3 { font-size: 1rem; font-weight: 700; color: var(--text-primary); margin-bottom: 6px; }
     .attack-overview p  { font-size: 0.875rem; color: var(--text-secondary); line-height: 1.65; }
 
@@ -221,13 +223,13 @@
     .step-circle { width: 36px; height: 36px; border-radius: 50%; background: var(--border); color: var(--text-muted); display: flex; align-items: center; justify-content: center; font-weight: 700; font-size: 0.875rem; border: 2px solid var(--border); transition: all 0.15s; z-index: 1; flex-shrink: 0; }
     .step-line { width: 2px; background: var(--border); flex: 1; min-height: 16px; }
     .step-item:last-child .step-line { display: none; }
-    .step-item.active .step-circle { background: #2b6cb0; color: white; border-color: #2b6cb0; }
-    .step-item:hover .step-circle { border-color: #3182ce; }
+    .step-item.active .step-circle { background: var(--accent); color: white; border-color: var(--accent); }
+    .step-item:hover .step-circle { border-color: var(--accent); }
     .step-content { flex: 1; padding: 2px 0 18px 14px; }
-    .step-desc { font-size: 0.875rem; color: var(--text-primary); line-height: 1.6; padding: 10px 14px; border-radius: 8px; border: 1.5px solid var(--border); background: var(--step-desc-bg); transition: all 0.15s; }
+    .step-desc { font-size: 0.875rem; color: var(--text-primary); line-height: 1.6; padding: 10px 14px; border-radius: 8px; border: 1px solid var(--border); background: var(--step-desc-bg); transition: all 0.15s; }
     .step-item.active .step-desc { border-color: var(--step-active-bd); background: var(--step-active-bg); }
     .step-item:hover .step-desc { border-color: #c3dafe; }
-    .step-detail { display: none; margin-top: 10px; padding: 14px; background: var(--bg-panel); border: 1.5px solid var(--border); border-radius: 8px; }
+    .step-detail { display: none; margin-top: 10px; padding: 14px; background: var(--bg-panel); border: 1px solid var(--border); border-radius: 8px; }
     .step-item.active .step-detail { display: block; }
     .mitre-row { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 6px; }
     .mitre-tactic    { background: #e9d8fd; color: #553c9a; padding: 4px 10px; border-radius: 4px; font-size: 0.78rem; font-weight: 700; font-family: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace; }
@@ -238,7 +240,7 @@
 
     .attack-diagram-panel { position: sticky; top: 20px; }
     .attack-diagram-title { font-size: 0.78rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text-muted); margin-bottom: 10px; }
-    .legend-note { margin-top: 10px; padding: 10px 14px; border-radius: 8px; font-size: 0.875rem; border: 1.5px solid; }
+    .legend-note { margin-top: 10px; padding: 10px 14px; border-radius: 8px; font-size: 0.875rem; border: 1px solid; }
 
     /* ── Attack chain two-column layout ── */
     .attack-chain-body {
@@ -276,7 +278,7 @@
       font-size: 0.875rem; font-weight: 600; cursor: pointer;
       transition: background 0.15s;
     }
-    .attack-nav-btn:hover:not([disabled]) { background: #2b6cb0; }
+    .attack-nav-btn:hover:not([disabled]) { background: var(--accent); }
     .attack-nav-btn[disabled] { opacity: 0.3; cursor: not-allowed; }
 
     /* ── Mitigations ── */
@@ -296,12 +298,12 @@
     /* TB filter active banner */
     .mit-tb-banner {
       display: none; align-items: center; gap: 10px; flex-wrap: wrap;
-      background: var(--bg-subtle); border: 1.5px solid #9ae6b4; border-radius: 8px;
+      background: var(--bg-subtle); border: 1px solid #9ae6b4; border-radius: 8px;
       padding: 10px 14px; margin-bottom: 16px; font-size: 0.875rem; color: var(--text-muted);
     }
     .mit-tb-banner strong { color: var(--text-primary); }
     .mit-tb-banner-clear {
-      margin-left: auto; background: none; border: 1.5px solid #9ae6b4;
+      margin-left: auto; background: none; border: 1px solid #9ae6b4;
       border-radius: 6px; padding: 3px 10px; cursor: pointer; font-size: 0.82rem;
       font-weight: 600; color: #276749; transition: all 0.15s;
     }
@@ -315,15 +317,15 @@
       letter-spacing: 0.08em; color: var(--text-faint); margin-bottom: 6px; }
     .mit-expand-content p { font-size: 0.875rem; color: var(--text-secondary); line-height: 1.65; }
     .mit-expand-icon { display: inline-block; transition: transform 0.2s; color: var(--text-faint); font-size: 0.75rem; }
-    .mit-expand-icon.open { transform: rotate(180deg); color: #2b6cb0; }
-    .data-row:hover .mit-expand-icon { color: #2b6cb0; }
+    .mit-expand-icon.open { transform: rotate(180deg); color: var(--accent); }
+    .data-row:hover .mit-expand-icon { color: var(--accent); }
 
 
-    .filter-panel { background: var(--bg-subtle); border: 1.5px solid var(--border); border-radius: 10px; padding: 18px 20px; margin-bottom: 20px; }
+    .filter-panel { background: var(--bg-subtle); border: 1px solid var(--border); border-radius: 12px; padding: 18px 20px; margin-bottom: 20px; }
     .filter-panel h3 { font-size: 0.8rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text-secondary); margin-bottom: 12px; }
     .filter-chips { display: flex; flex-wrap: wrap; gap: 8px; }
     /* "All" stays a compact pill */
-    .filter-chip { display: flex; flex-direction: column; gap: 2px; padding: 7px 14px; border-radius: 8px; border: 1.5px solid var(--fc-stroke,#e2e8f0); cursor: pointer; background: var(--bg-panel); transition: all 0.15s; user-select: none; text-align: left; }
+    .filter-chip { display: flex; flex-direction: column; gap: 2px; padding: 7px 14px; border-radius: 8px; border: 1px solid var(--fc-stroke,#e2e8f0); cursor: pointer; background: var(--bg-panel); transition: all 0.15s; user-select: none; text-align: left; }
     .filter-chip:hover { border-color: var(--fc-stroke,#3182ce); background: var(--fc-fill,#ebf8ff); }
     .filter-chip:hover .fc-name { color: #1a202c; }
     .filter-chip:hover .fc-id   { color: var(--fc-stroke, #2b6cb0); }
@@ -338,12 +340,12 @@
     .filter-chip .fc-name { font-size: 0.82rem; font-weight: 600; color: var(--text-primary); line-height: 1.3; }
     .filter-chip.active .fc-name { color: white; }
 
-    .stats-row { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 20px; }
-    .stat-chip { background: var(--bg-panel); border: 1.5px solid var(--border); border-radius: 10px; padding: 12px 18px; }
-    .stat-chip .sc-num { font-size: 1.5rem; font-weight: 700; color: #2b6cb0; }
-    .stat-chip .sc-label { font-size: 0.72rem; color: var(--text-faint); margin-top: 2px; }
+    .stats-row { display: flex; gap: 16px; flex-wrap: wrap; margin-bottom: 24px; }
+    .stat-chip { background: var(--bg-panel); border: 1px solid var(--border); border-radius: 12px; padding: 20px 24px; flex: 1; min-width: 120px; }
+    .stat-chip .sc-num { font-size: 2rem; font-weight: 700; color: var(--accent); line-height: 1; }
+    .stat-chip .sc-label { font-size: 0.78rem; color: var(--text-muted); margin-top: 6px; font-weight: 500; }
 
-    .table-wrap { overflow-x: auto; border-radius: 8px; border: 1.5px solid var(--border); }
+    .table-wrap { overflow-x: auto; border-radius: 8px; border: 1px solid var(--border); }
     table { width: 100%; border-collapse: collapse; font-size: 0.875rem; }
     thead th { background: var(--thead-bg); color: var(--thead-color); padding: 12px 16px; text-align: left; font-weight: 600; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.05em; position: sticky; top: 0; z-index: 1; }
     tbody td { padding: 14px 16px; border-bottom: 1px solid var(--border); vertical-align: top; line-height: 1.6; background: var(--bg-panel); color: var(--text-primary); }
@@ -353,11 +355,11 @@
     tbody tr.data-row.row-active td { background: var(--row-active); }
 
     .mit-diagram-panel { position: sticky; top: 20px; }
-    .tb-info-box { margin-top: 8px; padding: 12px 14px; border-radius: 8px; border: 1.5px solid var(--border); font-size: 0.875rem; color: var(--text-secondary); line-height: 1.6; display: none; }
+    .tb-info-box { margin-top: 8px; padding: 12px 14px; border-radius: 8px; border: 1px solid var(--border); font-size: 0.875rem; color: var(--text-secondary); line-height: 1.6; display: none; }
     .tb-info-box strong { display: block; color: var(--text-primary); margin-bottom: 4px; font-size: 0.9rem; }
 
     .tb-tag { display: inline-block; padding: 2px 8px; border-radius: 6px; font-size: 0.78rem; font-weight: 700; margin: 1px; }
-    hr.divider { border: none; border-top: 1.5px solid #e2e8f0; margin: 20px 0; }
+    hr.divider { border: none; border-top: 1px solid #e2e8f0; margin: 20px 0; }
   </style>
 </head>
 <body>
@@ -404,7 +406,7 @@
         <div class="diagram-wrapper">
           <svg id="arch-svg" style="font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif; display:block;"></svg>
         </div>
-        <div style="margin-top:14px; padding:14px 16px; background:var(--bg-subtle); border-radius:8px; border:1.5px solid var(--border);">
+        <div style="margin-top:14px; padding:14px 16px; background:var(--bg-subtle); border-radius:8px; border:1px solid var(--border);">
           <div class="section-label" style="margin-bottom:10px;">Trust Boundaries — click any label to learn more</div>
           <div class="tb-legend" id="tb-legend"></div>
         </div>
